<h1>All Departments</h1>
<table>
	<tr>
		<th>ID</th>
		<th>Name</th>
		<th>Details</th>
	</tr>
	<% @departments.each do|department|%>
	<tr>
		<td><%= department.id %></td>
		<td><%= department.name %></td>
		<td>
			<%= link_to "Edit","/departments/#{department.id}/edit" %>
			<%= link_to "Delete","/departments/#{department.id}" , method: "delete"%>
			<%= link_to "all employees",employees_path(id: department.id)%>
	    </td>

	</tr>
	<%end%>
<table>

<%= link_to "Add Department",new_department_path %>

